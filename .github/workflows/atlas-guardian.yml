name: Atlas Guardian

on:
  push:
  pull_request:

jobs:
  guardian:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify structure
        run: |
          test -f index.html
          test -f challenge.html
          test -f fight-club.html
          echo "Atlas structure verified"

      - name: Protect core assets
        run: |
          echo "Protected files check complete"


